# API Environment Variables

# JWT Secret for session tokens
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Frontend URL
FRONTEND_URL=http://localhost:4321

# LDAP/FreeIPA Configuration
# Primary LDAP server URL (LDAPS recommended for security)
LDAP_URL=ldaps://iam.allware.cl:636

# Fallback LDAP URL (without TLS)
LDAP_FALLBACK_URL=ldap://iam.allware.cl:389

# Base DN for your FreeIPA domain
LDAP_BASE_DN=dc=allware,dc=cl

# User search base (where users are stored)
LDAP_USER_SEARCH_BASE=cn=users,cn=accounts,dc=allware,dc=cl

# Username attribute (default: uid for FreeIPA)
LDAP_USERNAME_ATTR=uid

# Optional: Service account for searching users
# Leave empty for simple bind authentication
LDAP_BIND_DN=
LDAP_BIND_PASSWORD=

# Optional: Path to CA certificate for LDAPS
# Typically /etc/ipa/ca.crt on FreeIPA-enrolled hosts
LDAP_CA_CERT_PATH=

# Connection timeout in milliseconds
LDAP_CONNECT_TIMEOUT=5000

# TLS options (set to 'false' to allow self-signed certificates)
LDAP_TLS_REJECT_UNAUTHORIZED=true
